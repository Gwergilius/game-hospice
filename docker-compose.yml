services:
  game-hospice:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ${GAMES_PATH:-C:/DOS/Games}:/games:ro
    environment:
      - GamesPath=/games
      - ASPNETCORE_URLS=http://+:8080
